import LoginCard from "./components/loginCard";

export default function Home() {
  return (
    <div className="flex bg-sky-500 relative h-screen overflow-clip items-center justify-center">
      <LoginCard />
    </div>
  );
}
